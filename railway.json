{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd frontend && npm ci && npx prisma generate && npm run build",
    "watchPatterns": [
      "frontend/**"
    ]
  },
  "deploy": {
    "startCommand": "cd frontend && npx prisma migrate deploy && npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
